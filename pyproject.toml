[project]
name = "underground-live-map"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"

# requirements.txt is the real dependency manifest (installed via
# uv pip install --python .venv/Scripts/python.exe -r requirements.txt, see README).
# This file is deliberately just packaging metadata, not a second dependency list.
