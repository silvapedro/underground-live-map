fastapi>=0.116.1,<1.0.0
uvicorn[standard]>=0.35.0,<1.0.0
jinja2>=3.1.6,<4.0.0
requests>=2.32.5,<3.0.0
python-dotenv>=1.0.0,<2.0.0
# Verifies TLS against the OS trust store. Required on machines with TLS-intercepting
# antivirus/proxies, or whose Windows cert store contains a cert OpenSSL rejects.
truststore>=0.10.0,<1.0.0
pytest>=8.0.0,<9.0.0
